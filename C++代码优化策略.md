代码优化策略

1. 选择合适的算法  时间复杂度 空间复杂度 
2.代码简洁简单，便于读。编译器好处理
3.注意内存、数据读写限制程序的速度。计算机存储机制一层一层。
磁盘/SSD，读数据搬到硬盘，再往cash。如果内存地址是连续的，读取很快。
4.避免内存拷贝，代码更加复杂。两个对象同一块数据如何处理。
5.避免打印，打印会诸塞程序的执行。
6.Table lookup  三角函数慢，计算机擅长整数浮点等。
    精度不高可以用查表法。
    预先算出来放到数组里，0度对用**，1度对用**。
7.SIMD,OpenMP
    装载到寄存器（理论上提速），一次处理多个数据。